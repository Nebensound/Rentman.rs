version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
    # Raise the Cargo.toml requirements alongside the lockfile so published
    # releases document the dependency versions the crate is tested against.
    versioning-strategy: "increase"
    groups:
      # Collect routine version bumps in one PR; majors stay separate so
      # breaking changes get their own review.
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
